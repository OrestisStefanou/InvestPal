[project]
name = "agent-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
    "pydantic-settings>=2.0.0",
    "fastapi[standard]>=0.122.0",
    "langchain>=1.1.2",
    "langchain-openai>=1.1.0",
    "langchain-google-genai>=3.2.0",
    "langchain-mcp-adapters>=0.1.14",
    "langchain-anthropic>=1.2.0",
    "fastmcp>=3.0.0b2",
    "croniter>=6.2.2",
    # Capped below 0.7: the sync engine now owns on-disk metadata next to
    # investpal.db, and pyturso is alpha, so the engine should not change under a
    # synced database as a side effect of `uv lock -U`. Bump deliberately.
    "pyturso>=0.6.1,<0.7",
    "fastembed>=0.8.0",
]
